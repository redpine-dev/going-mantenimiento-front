import { type ComponentPropsWithoutRef, type ComponentRef } from 'react';

type TableFooterProps = ComponentPropsWithoutRef<'tfoot'>;

type TableFooterRef = ComponentRef<'tfoot'>;

export type { TableFooterProps, TableFooterRef };
