import { type ComponentPropsWithoutRef, type ComponentRef } from 'react';

type TableHeadProps = ComponentPropsWithoutRef<'th'>;

type TableHeadRef = ComponentRef<'th'>;

export type { TableHeadProps, TableHeadRef };
